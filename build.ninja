include_dir = ninja

# Flags common to all builds, regardless of toolchain or hardware target.
common_cflags = -Wall -Werror -Isrc

include $include_dir/all.ninja

# Build all boards by default.
default all_boards
